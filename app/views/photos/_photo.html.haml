= content_tag :li, id: photo.id do
  = link_to image_tag(photo.image_url(:cover), class: "img-polaroid img-rounded"), photo
  .controls
    = link_to 'Set as Cover', set_cover_album_photo_path(@album, photo), method: :post, class: 'btn btn-sml'

